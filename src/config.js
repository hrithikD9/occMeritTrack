// Configuration
export const CONFIG = {
  // LocalStorage keys (keeping for backward compatibility, will be replaced with backend)
  STORAGE_KEY: 'occ_merit_track_candidates',
  ROLE_STORAGE_KEY: 'occ_merit_track_role',
};
